<!-- Seeded by Bower's scaffold because this project had no AGENTS.md. It is
     project-owned from here on — the scaffold never edits it again. Claude Code
     reaches this file through CLAUDE.md's `@AGENTS.md` include. -->

# Project instructions

This is a Bower-framework project. **Before any Bower work — any `/b-*` or `$b-*` skill, any question about project state, any change to `docs/` — read `_bower/framework.md` in full.** It is the router for how this project is designed, documented, and changed; acting without it produces non-conformant work.

## Project-Specific Code Standards
