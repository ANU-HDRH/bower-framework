---
name: t-args
description: Fixture command with an argument binding — exercises the marker substitution path.
arguments: the target thing
---

# Fixture With Arguments

<!-- bower:arguments -->

The marker above is the only line the generator rewrites. On Claude Code it becomes the slash-command argument binding; in a Codex skill it becomes the invoking-message wording. Everything else in this body is copied verbatim to both.
