# Adoption ledger

Cross-cutting choices found in code whose rationale could not be attributed.
Open items only. Each exits by resolve, remediate, or dismiss — all three delete
the line.

- `src/found.ts:14` · Why is the retry count 7 rather than the library default?
- `docs/../src/found.ts:40` · Why does this path bypass the shared client?
