# Constitution

## Testing

Tests live in `tests/`. The runner is `node --test`.

## Not yet in force

Intended, but not true of the repo today. Agents: treat these as non-existent.

- Contract tests at every module boundary — only `clean` has one.
