# Problem space

Day-1 framing. A Bower document, so it belongs above the rail's break and gets
no project strap — unlike `operations/runbook.md` in the same tree.

## The problem

Documentation drift is invisible until someone reads two files at once.
