# boldorder — module status

The build-order-unparsed condition: numbered entries that do not parse, because
the feature names are decorated in a way the schema does not use. The roster
must read as empty *and* say why, rather than silently cascading.

## Module integration

Test: not yet defined — ⏸
Notes: Nothing asserted yet.

## Build order

1. **bold-feature** — ⏸
2. **another-bold** — ⏸
