# stale-pointer — ✓

Built and verified. Never compressed to the terminal form, so it still reads as
a live resumption snapshot and still points at another feature's work. Written
with the inline bold `Next move:` form rather than a `## Next move` section —
both spellings occur, and reading only the section made this whole class
invisible.

**Next move:** `Run /b-feature ghost-feature`
