# Scope

## Current scope

A synthetic project whose only job is to contain one instance of every
documentation-drift condition the viewer checks for.

## Current non-goals

Being a real project.

## Success criteria

- The clean module's every feature and its integration test are complete.
  _Delivered by: clean_
- The drifted module reaches completion.
  _Delivered by: drifted_
- A criterion with no delivering module, which cannot be derived.
- A criterion pointing at a module that no longer exists.
  _Delivered by: dissolved-module_
- ⏸ A criterion still carrying a status marker, which v0.24 removed.
  _Delivered by: clean_
